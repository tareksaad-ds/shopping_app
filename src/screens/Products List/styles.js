import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container:{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    card: {
        width: '47%',
        padding:5,
        borderColor: "#cccccc",
        borderWidth: 0.6,
        marginTop:10,
        borderRadius: 5,
        marginHorizontal:5
    },
})